<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <AssemblyName>lyrc</AssemblyName>
    <RootNamespace>Lyric.Cli.Compiler</RootNamespace>
    <Description>The Lyric compiler: source to .lyrbc.</Description>
  </PropertyGroup>

  <ItemGroup>
    <!--
      Two references, and neither is the runtime: 'lyrc' executes nothing.
      tests/Lyric.Tests.Cli checks that lyrrt.dll never appears here.
    -->
    <ProjectReference Include="..\Lyric.Core\Lyric.Core.csproj" />
    <ProjectReference Include="..\Lyric.Frontend\Lyric.Frontend.csproj" />
    <!-- The runtime is the compiler's 'comptime' evaluator; nothing else of it is used here. -->
    <ProjectReference Include="..\Lyric.Vm\Lyric.Vm.csproj" />
  </ItemGroup>

</Project>
