import std.build { executable };

/// What `lyric build` runs. Every artifact is compiled whole from its entry file; nothing is
/// compiled while this function runs, so a field set on the next line still applies. The
/// program lands in out/<profile>/__name__.lyrbc.
pub fn build() {
    executable("__name__", "src/main.lyr");
}
