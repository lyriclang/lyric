# The .NET version comes from global.json and the SDK preinstalled on the runner, not from
# actions/setup-dotnet.
#
# Every job that used the action downloaded its archive from codeload and then a 240 MB SDK. On
# 2026-08-17 that download returned 429 and 503 eight times across the workflows, three of them
# during a release, each time failing in "Set up job" before anything was built. Nothing in this
# repository could retry its way out of it.
#
# global.json states the version, so a runner without a matching SDK fails with a message that says
# so rather than with a target-framework error further along.
name: CI

on:
  push:
    branches: [main, 'feature/**', 'fix/**']
  pull_request:
    branches: [main]

permissions:
  contents: read

# Successive pushes to one branch collapse: the newer run cancels the older, so a slice pushed
# twice in a minute is verified once. The EVENT is part of the key on purpose. Unifying push and
# pull_request would halve the load again — a push to a branch with an open PR does fire both —
# but the run that loses is reported as a cancelled check ON the pull request, which reads as a
# failure and blocks a merge. A duplicate run is cheaper than a red cross that means nothing.
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build and test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Test
        run: dotnet test --configuration Release --no-build --verbosity normal --logger "trx;LogFileName=test-results.trx"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results-${{ matrix.os }}
          path: '**/TestResults/*.trx'
          retention-days: 7

  compiled:
    # The SECOND execution engine, held to the answers of the first. LYRIC_JIT=1 compiles every
    # function the emitter accepts, in every test, and the suite has to give the same results as
    # interpreted — which is the only thing that makes a compiler trustworthy. Without a job of
    # this shape the comparison would have to be written into each test by hand, and would cover
    # whichever tests somebody remembered.
    #
    # Linux only, and deliberately: the emitter is platform-independent IL and a second OS would
    # buy a runtime difference, not a Lyric one. A debugged or metered run stays interpreted even
    # here, so the tests that pin THOSE keep pinning them.
    name: Build and test (compiled)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Test with the compiler on
        env:
          LYRIC_JIT: '1'
        run: dotnet test --configuration Release --no-build --verbosity normal

  spec-sync:
    # docs/Grammar.md and docs/Bytecode.md are MIRRORS of the spec repository's chapters 02
    # and 13: everything below the '<!-- sync:body -->' marker must be byte-identical. This
    # job is what makes the mirror model honest — drift fails the build instead of shipping.
    name: Spec mirror sync
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Checkout the specification
        uses: actions/checkout@v7
        with:
          repository: lyriclang/lyric-spec
          path: spec-repo

      - name: Diff the mirrored bodies
        run: |
          status=0
          check() {
            sed -n '/^<!-- sync:body -->$/,$p' "$1" > /tmp/mirror.txt
            sed -n '/^<!-- sync:body -->$/,$p' "$2" > /tmp/canonical.txt
            if ! diff -u /tmp/canonical.txt /tmp/mirror.txt; then
              echo "::error file=$1::mirror drifted from $2"
              status=1
            fi
          }
          check docs/Grammar.md  spec-repo/spec/02-grammar.md
          check docs/Bytecode.md spec-repo/spec/13-bytecode.md
          exit "$status"

  conformance:
    # The specification's suite runs against the WORKING TREE, not only against releases: the
    # tree is the candidate for the next release, and a candidate that breaks a spec sentence
    # should fail here, not after tagging. The version from Directory.Build.props decides which
    # '//! since:' cases apply — the tree claims what it is.
    name: Conformance gate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Checkout the specification
        uses: actions/checkout@v7
        with:
          repository: lyriclang/lyric-spec
          path: spec-repo

      - name: Build the toolchain
        run: dotnet build --configuration Release

      - name: Run the suite against the working tree
        run: |
          VERSION=$(sed -n 's:.*<Version>\(.*\)</Version>.*:\1:p' Directory.Build.props)
          echo "working tree claims $VERSION"
          python3 spec-repo/tools/run_conformance.py \
            --lyrc src/Lyric.Cli/bin/Release/net10.0/lyrc \
            --lyrvm src/Lyric.Cli/bin/Release/net10.0/lyrvm \
            --stdlib stdlib \
            --toolchain-version "$VERSION"

  publish:
    # Runs after the tests so no artifact is produced from a red build.
    name: Publish toolchain
    # Both engines, not one: an artifact whose compiled run disagrees with its interpreted one
    # is an artifact nobody should install.
    needs: [build-and-test, compiled]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Publish all four binaries into one directory
        run: dotnet msbuild build/publish.proj

      - name: Show what gets shipped
        run: ls -la artifacts/publish

      - name: Upload toolchain
        uses: actions/upload-artifact@v7
        with:
          name: lyric-toolchain
          path: artifacts/publish
          retention-days: 14
