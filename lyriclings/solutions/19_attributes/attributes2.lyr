// attributes2.lyr — solution
//
// The promise is kept: the deprecated function is gone. (Keeping it with `until = "99.0"`
// would compile too — but that is a new promise, not the old one.)

import std.io.console { println };

pub fn renew(): int {
    return 2;
}

fn main(): int {
    println(f"{renew()}");
    return 0;
}
