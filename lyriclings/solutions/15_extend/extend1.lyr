// extend1.lyr — solution

import std.io.console { println };

extend int {
    fn isEven(): bool { return this % 2 == 0; }
}

fn main(): int {
    for (n in [4, 7]) {
        println(f"{n} is " + (if (n.isEven()) "even" else "odd"));
    }
    return 0;
}
